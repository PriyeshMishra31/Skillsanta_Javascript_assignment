// Write a JavaScript program to check whether a given integer is
// within range of 50-100 or not.

let range = prompt("Enter the Range of numbers");
 
if(range > 50 && range < 100)
{
    document.write("The Number is on range");

}
else{
    document.write("The Number is not in range");
}